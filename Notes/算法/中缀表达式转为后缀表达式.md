```c
#include<stdio.h>

#include<ctype.h>

#include<string.h>

  

int priority(char a)

{

    if(a=='+'||a=='-') return 1;

    else if(a=='*'||a=='/') return 2;

    return 0;

}

  

int isfuhao(char c)

{

    if(c=='+'||c=='-'||c=='*'||c=='/') return 1;

    else return 0;

}

  

int main()

{

    char ch[100];

    char stack[100];

    scanf("%s",ch);

    int n=strlen(ch);

    int first=1;

    int top=-1;

    int i=0;

    while(i<n)

    {

        // 外层if：完全保留你原来的代码，一点不改

        if(isdigit(ch[i])||ch[i]=='.'||(ch[i]=='-'||ch[i]=='+')&&(i==0||isfuhao(ch[i-1])||ch[i-1]=='('))        

        {

            if(!first) printf(" ");

            first=0;

            // 只改这里！内层while删掉+，只保留负号绑定数字

            while(isdigit(ch[i])||ch[i]=='.'||(ch[i]=='-')&&(i==0||ch[i-1]=='('||isfuhao(ch[i-1])))

            {

                printf("%c",ch[i]);

                i++;

            }

        }

        else if(ch[i]=='(')

        {

            stack[++top]=ch[i];

            i++;

        }

        else if(ch[i]==')')

        {

            while(stack[top]!='(')

            {

                printf(" %c",stack[top--]);

            }

            top--;

            i++;

        }

        else

        {

            // 兜底：开头单独的+直接忽略

            if(ch[i] == '+' && i == 0){

                i++;

                continue;

            }

            while(top!=-1&&priority(stack[top])>=priority(ch[i]))

            {

                printf(" %c",stack[top--]);

            }

            stack[++top]=ch[i];

            i++;

        }

    }

    while(top!=-1) printf(" %c",stack[top--]);

    return 0;

}


