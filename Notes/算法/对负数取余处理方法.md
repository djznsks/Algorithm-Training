
```cpp
// 统计每个余数出现的次数

        for (int num : arr) {

            int remainder = num % k;

            // 处理负数，确保余数在 [0, k-1] 范围内

            if (remainder < 0) remainder += k;

            freq[remainder]++;

        }
