

```cpp

vector<int> sorted_pre = pre;

        sort(sorted_pre.begin(), sorted_pre.end());//排序

        sorted_pre.erase(unique(sorted_pre.begin(), sorted_pre.end()), sorted_pre.end());//去重
        
        //常结合二分用来快速查询pre数组元素中的大小排名


